# GhidrAI 设计哲学

## 一、产品定位

GhidrAI 是一款面向逆向工程、二进制分析、固件分析与安全研究的 TUI 逆向工作台。

它不是单纯的反汇编器，也不是简单套壳的 AI 聊天工具，而是一个把 **静态分析、动态调试、符号恢复、字符串分析、函数重命名、结构体推断、项目记忆与 AI 辅助理解** 统一到终端里的逆向瑞士军刀。

GhidrAI 的目标是让逆向过程从“到处切工具、到处复制粘贴、到处手动记录”，变成一种更连续、更清晰、更可复用的分析体验。

它服务的不是“破解”本身，而是服务于理解：

理解一个二进制文件如何组织，理解一个函数为什么存在，理解一段汇编背后的业务意图，理解固件、协议、程序结构与系统行为。

## 二、核心理念：让逆向结果更舒服

传统逆向工具的问题不只是功能不够，而是分析结果经常不够舒服。

所谓“不舒服”，包括：

1. 信息太碎：函数、字符串、交叉引用、注释、推测结论散落在不同窗口。
2. 认知负担高：用户需要同时记住地址、调用关系、变量含义和上下文。
3. 工具链割裂：反汇编、调试、字符串提取、文件识别、符号分析、AI 解释往往分属不同工具。
4. 结果不可持续：今天分析出的判断，明天重新打开项目还要重新整理。
5. AI 结果不可控：AI 容易把猜测当事实，导致后续分析越来越偏。

GhidrAI 要解决的不是“让 AI 替你逆向”，而是让 AI 成为逆向过程里的第二大脑。

它应该帮用户整理信息、压缩上下文、解释复杂逻辑、生成假设、标记置信度，并在用户确认之后沉淀成项目记忆。

逆向的核心体验应该从：

> 我在一堆地址和工具之间来回跳转

变成：

> 我在一个连续的分析空间里逐步理解目标系统

## 三、TUI 优先：为高频操作而生

GhidrAI 选择 TUI，不是因为它比 GUI 更酷，而是因为逆向本身就是一种高频、深度、键盘密集型工作。

逆向工程师真正需要的是：

* 快速跳转地址
* 快速搜索字符串
* 快速查看交叉引用
* 快速重命名函数
* 快速写注释
* 快速切换反汇编、伪代码、十六进制、调用图和 AI 总结
* 快速把一个判断保存为项目结论

这些操作不应该依赖鼠标来回点，也不应该被复杂窗口打断。

GhidrAI 的 TUI 应该遵循：

1. **键盘优先**：核心操作必须能用键盘完成。
2. **命令面板优先**：任何功能都可以通过 Command Palette 调出。
3. **面板可组合**：反汇编、字符串、调用图、AI 解释、注释、十六进制视图可以自由组合。
4. **低打扰反馈**：分析进度、AI 推理、索引状态应该清晰显示，但不能打断用户。
5. **可复制、可导出、可复现**：所有分析结果都应该能沉淀为报告、笔记或结构化数据。

TUI 不是简陋版 GUI，而是专业工具的高速驾驶舱。

## 四、瑞士军刀式架构

GhidrAI 不应该重复制造所有轮子，而应该把优秀的 C++ / Rust 逆向工具能力整合进统一工作流。

它的定位是：

> 一个统一入口，调度不同工具；一个项目空间，沉淀所有结果；一个 TUI 界面，承载完整逆向流程。

GhidrAI 应该支持多类能力：

1. 文件识别
   识别 ELF、PE、Mach-O、固件镜像、裸 bin、压缩包、可疑资源段。

2. 静态分析
   解析 section、segment、符号表、导入表、导出表、字符串、交叉引用、函数边界。

3. 反汇编
   支持 x86/x64、ARM、AArch64、RISC-V 等常见架构。

4. 动态调试
   对接调试器能力，支持断点、寄存器、内存查看、调用栈和运行状态记录。

5. 字符串与资源分析
   提取字符串、URL、路径、协议字段、错误信息、配置片段和可疑常量。

6. 函数理解
   自动生成函数摘要、参数猜测、返回值猜测、调用关系说明。

7. 项目记忆
   保存用户命名、注释、AI 假设、确认事实、结构体推断和分析报告。

8. AI 辅助
   解释汇编、总结函数、生成假设、推断协议、辅助重命名、整理报告。

9. 插件系统
   允许外部工具通过 adapter 接入，形成可扩展的逆向工具集合。

GhidrAI 的价值不在于“自己做完所有事”，而在于把所有能力整合成一个统一、顺滑、可信的工作流。

## 五、速度哲学：快，是逆向体验的一部分

逆向工具不能只是功能丰富，还必须足够快。

因为逆向过程本质上是高频探索：

用户会不断搜索、跳转、比较、回退、重命名、看引用、看字符串、看函数调用图。如果每一步都卡顿，分析思路就会被打断。

GhidrAI 的性能哲学是：

1. **Rust 负责核心调度与数据安全**
   Rust 适合承担项目管理、TUI 渲染、索引缓存、并发任务、内存安全和插件调度。

2. **C++ 工具负责高性能底层能力**
   对于成熟的反汇编、调试、符号分析、二进制解析能力，可以通过 C/C++ 工具链或 FFI/CLI adapter 接入。

3. **增量分析优先**
   不应该每次打开项目都重新分析全部内容。已经分析过的函数、字符串、引用、AI 结果应该缓存。

4. **异步任务优先**
   大文件分析、AI 总结、全局索引、交叉引用计算应该异步执行，用户可以先开始浏览。

5. **结果流式展示**
   工具不必等全部分析完成再给结果。先展示文件结构、入口点、字符串和初步函数，再逐步补全复杂分析。

6. **缓存即体验**
   用户第二次打开项目时，应该明显感觉“它记得我上次分析到哪里了”。

速度不是技术炫耀，而是让逆向思维不断流的基础。

## 六、AI 不是结论，而是可管理的假设

GhidrAI 的 AI 设计必须克制。

逆向分析中，AI 最危险的地方不是答错，而是把不确定的内容说得很确定。

因此 GhidrAI 的 AI 输出必须区分三类信息：

1. **Fact：已确认事实**
   来自二进制结构、符号表、字符串引用、用户确认或确定性分析。

2. **Hypothesis：分析假设**
   AI 根据上下文推测出的函数用途、协议格式、加密逻辑或业务含义。

3. **Question：待验证问题**
   AI 认为需要用户进一步确认的疑点。

例如：

* 这个函数调用了 memcpy 和 CRC 表，可能与数据校验有关。
* 这个字符串出现在登录错误路径中，可能属于认证逻辑。
* 这个结构体字段偏移疑似代表状态位，但需要动态调试验证。

GhidrAI 不能把这些直接写成事实，而应该提供置信度、证据来源和确认入口。

用户确认之后，假设才能进入项目记忆。

## 七、项目记忆：让每次分析都能积累

GhidrAI 的核心能力之一是 project memory。

传统逆向工具保存的是工程文件，GhidrAI 保存的不只是工程文件，而是分析过程中的知识。

它应该记住：

* 用户给函数起过什么名字
* 哪些函数已经分析过
* 哪些结构体字段被推断出来
* 哪些 AI 结论被用户确认
* 哪些地方仍然是猜测
* 哪些字符串和函数关系重要
* 哪些地址需要后续重点检查
* 当前项目的整体分析进度

项目记忆应该是可读、可导出、可追踪的。

推荐结构：

```text
.ghidrai/
├── project.sqlite
├── symbols.json
├── comments.md
├── hypotheses.json
├── confirmed.json
├── reports/
└── cache/
```

其中：

* `symbols.json` 保存函数名、变量名、地址映射。
* `comments.md` 保存人类可读笔记。
* `hypotheses.json` 保存 AI 推测。
* `confirmed.json` 保存用户确认事实。
* `project.sqlite` 保存结构化索引和查询数据。
* `cache/` 保存反汇编、字符串、交叉引用、AI 摘要缓存。

这样 GhidrAI 才不是一次性工具，而是一个长期陪伴项目推进的逆向工作台。

## 八、好的体验：不是功能多，而是分析链短

GhidrAI 要追求的体验不是“按钮很多”，而是“分析链足够短”。

用户看到一个可疑字符串时，应该能快速完成：

1. 查看字符串。
2. 跳转引用函数。
3. 查看函数上下文。
4. 让 AI 总结函数作用。
5. 查看调用者和被调用者。
6. 重命名函数。
7. 写入注释。
8. 标记为已确认或待验证。
9. 继续跳到下一个相关点。

整个过程应该在一个界面里完成，而不是在五个工具之间复制地址。

GhidrAI 的交互目标是：

> 用户的每一次操作都应该让项目变得更清楚。

## 九、结果输出：让逆向成果可交付

逆向的最终结果不应该只停留在工具内部。

GhidrAI 应该支持把分析结果输出为：

* Markdown 报告
* 函数列表
* 调用关系图
* 字符串引用表
* 协议字段推断
* 结构体说明
* 可疑函数清单
* AI 辅助分析摘要
* 项目复盘文档

这让 GhidrAI 不只是分析工具，也可以成为报告生成工具。

逆向成果应该能被自己复盘、被队友接手、被项目沉淀。

## 十、最终愿景

GhidrAI 的最终形态不是一个“AI 反汇编器”，而是一个面向 AI 时代的逆向工程操作系统。

它以 TUI 作为高速入口，以 Rust 作为安全高效的核心，以 C++ / Rust 工具链作为底层能力，以项目记忆作为长期知识库，以 AI 作为上下文整理和推理辅助。

它的目标不是替代逆向工程师，而是增强逆向工程师。

让工具负责整理、索引、关联、解释和记录；
让人负责判断、验证、决策和理解。

GhidrAI 的设计原则可以总结为一句话：

> 让逆向从碎片化的工具操作，变成连续、清晰、可积累的理解过程。
